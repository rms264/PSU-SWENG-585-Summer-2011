package gui;import java.awt.*;import java.awt.event.*;public class ImageFrame extends BaseFrame {  private static final long serialVersionUID = 1L;  MenuItem openGif_mi;  MenuItem default_mi;  MenuItem revert_mi;  MenuItem fitScreen_mi;  public ImageFrame(KahinduMenuBar menuBar, KahinduImage image) {    super(menuBar, image);    initialize();  }    private void initialize() {    initMenu();//    grabNumImage();    setVisible(true);    setSize(image.getWidth(), image.getHeight());    repaint();   }  private void initMenu() {    KahinduMenu fileMenu = new KahinduMenu("File",MenuEnum.FILE);    KahinduMenu openMenu = new KahinduMenu("Open",MenuEnum.OPEN);    try {      menuBar.addMenu(fileMenu);    } catch (Exception e1) {      // TODO Auto-generated catch block      e1.printStackTrace();    }    try {      menuBar.addMenu(MenuEnum.FILE, openMenu);    } catch (Exception e) {      // TODO Auto-generated catch block      e.printStackTrace();    }    openGif_mi = menuBar.addMenuItem(openMenu,"[g]if...");    default_mi = menuBar.addMenuItem(openMenu,"[d]efault image");    revert_mi = menuBar.addMenuItem(openMenu,"[r]evert");    fitScreen_mi = menuBar.addMenuItem(openMenu,"fitScreen");  }    		  public void fitScreen() {    Toolkit tk = Toolkit.getDefaultToolkit();    Dimension d	= tk.getScreenSize();    setSize(d.width, d.height);  }  public void actionPerformed(ActionEvent e) {	if (match(e, fitScreen_mi)) {		fitScreen();		return;	}	if (match(e, openGif_mi)) {		image.openGif();		return;	}	if (match(e, default_mi)) {		image.grabNumImage();		return;	}	if (match(e, revert_mi)) {		image.revert();		return;	}}   public void paint(Graphics g) {    if (image != null) {      Rectangle r = getBounds();      g.drawImage(image.getImage(), 0,0,r.width, r.height, 			 this);    }  }}